<wxs src="../../wxs/clsx.wxs" module="clsx"/>

<view class="{{rootClass}}">
  <image
    class="{{clsx(['bg-yellow-0', !user.avatar], iconClass)}}"
    src="{{user.avatar || '../../assets/images/anonymous.png'}}"
    bind:tap="tapAvatar"
  />
</view>

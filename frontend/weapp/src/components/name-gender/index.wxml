<wxs src="../../wxs/clsx.wxs" module="clsx"/>

<view class="flex items-baseline">
  <view class="{{nameClass}}">
    {{nickname}}
  </view>
  <view wx:if="{{gender > 0}}"
    class="ml-2 {{clsx(['if if-male text-blue-500', gender === 1], ['if if-female text-red-500', gender === 2])}}"/>
</view>
